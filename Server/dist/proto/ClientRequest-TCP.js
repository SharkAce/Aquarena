"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v4.23.3
// source: ClientRequest-TCP.proto
Object.defineProperty(exports, "__esModule", { value: true });
exports.ChatMessageRequest = exports.FindMatchRequest = exports.LoginRequest = exports.protobufPackage = void 0;
/* eslint-disable */
const _m0 = require("protobufjs/minimal");
exports.protobufPackage = "ClientRequest.TCP";
function createBaseLoginRequest() {
    return { username: "", password: undefined };
}
exports.LoginRequest = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.username !== "") {
            writer.uint32(10).string(message.username);
        }
        if (message.password !== undefined) {
            writer.uint32(18).bytes(message.password);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseLoginRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.username = reader.string();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.password = reader.bytes();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            username: isSet(object.username) ? globalThis.String(object.username) : "",
            password: isSet(object.password) ? bytesFromBase64(object.password) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.username !== "") {
            obj.username = message.username;
        }
        if (message.password !== undefined) {
            obj.password = base64FromBytes(message.password);
        }
        return obj;
    },
    create(base) {
        return exports.LoginRequest.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b;
        const message = createBaseLoginRequest();
        message.username = (_a = object.username) !== null && _a !== void 0 ? _a : "";
        message.password = (_b = object.password) !== null && _b !== void 0 ? _b : undefined;
        return message;
    },
};
function createBaseFindMatchRequest() {
    return { sessionId: new Uint8Array(0) };
}
exports.FindMatchRequest = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.sessionId.length !== 0) {
            writer.uint32(10).bytes(message.sessionId);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseFindMatchRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.sessionId = reader.bytes();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { sessionId: isSet(object.sessionId) ? bytesFromBase64(object.sessionId) : new Uint8Array(0) };
    },
    toJSON(message) {
        const obj = {};
        if (message.sessionId.length !== 0) {
            obj.sessionId = base64FromBytes(message.sessionId);
        }
        return obj;
    },
    create(base) {
        return exports.FindMatchRequest.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a;
        const message = createBaseFindMatchRequest();
        message.sessionId = (_a = object.sessionId) !== null && _a !== void 0 ? _a : new Uint8Array(0);
        return message;
    },
};
function createBaseChatMessageRequest() {
    return { sessionId: new Uint8Array(0), chatMessage: "" };
}
exports.ChatMessageRequest = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.sessionId.length !== 0) {
            writer.uint32(10).bytes(message.sessionId);
        }
        if (message.chatMessage !== "") {
            writer.uint32(18).string(message.chatMessage);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseChatMessageRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.sessionId = reader.bytes();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.chatMessage = reader.string();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            sessionId: isSet(object.sessionId) ? bytesFromBase64(object.sessionId) : new Uint8Array(0),
            chatMessage: isSet(object.chatMessage) ? globalThis.String(object.chatMessage) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.sessionId.length !== 0) {
            obj.sessionId = base64FromBytes(message.sessionId);
        }
        if (message.chatMessage !== "") {
            obj.chatMessage = message.chatMessage;
        }
        return obj;
    },
    create(base) {
        return exports.ChatMessageRequest.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b;
        const message = createBaseChatMessageRequest();
        message.sessionId = (_a = object.sessionId) !== null && _a !== void 0 ? _a : new Uint8Array(0);
        message.chatMessage = (_b = object.chatMessage) !== null && _b !== void 0 ? _b : "";
        return message;
    },
};
function bytesFromBase64(b64) {
    if (globalThis.Buffer) {
        return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
    }
    else {
        const bin = globalThis.atob(b64);
        const arr = new Uint8Array(bin.length);
        for (let i = 0; i < bin.length; ++i) {
            arr[i] = bin.charCodeAt(i);
        }
        return arr;
    }
}
function base64FromBytes(arr) {
    if (globalThis.Buffer) {
        return globalThis.Buffer.from(arr).toString("base64");
    }
    else {
        const bin = [];
        arr.forEach((byte) => {
            bin.push(globalThis.String.fromCharCode(byte));
        });
        return globalThis.btoa(bin.join(""));
    }
}
function isSet(value) {
    return value !== null && value !== undefined;
}
